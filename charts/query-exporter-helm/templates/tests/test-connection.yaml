---
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "query-exporter-helm.fullname" . }}-test-connection
  labels:
    {{- include "query-exporter-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "query-exporter-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
